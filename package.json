{
  "name": "grade_calc",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "3.3",
    "gh-pages": "^1.1.0",
    "react": "^16.2.0",
    "react-bootstrap": "^0.32.1",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build_and_deploy": "npm run build && gh-pages -d build"
  }
}
